import { ShowInRoleDirective } from './show-in-role.directive';

describe('ShowInRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowInRoleDirective();
    expect(directive).toBeTruthy();
  });
});
